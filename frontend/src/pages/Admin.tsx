import { useQuery } from '@tanstack/react-query';
import { Activity, Cpu, Database, Github, Server } from 'lucide-react';
import { endpoints } from '../services/api';
export function Admin(){const {data}=useQuery({queryKey:['status'],queryFn:endpoints.status,refetchInterval:30000}); const services=[['Docker',true,Server],['MySQL',data?.mysql,Database],['Qdrant',data?.qdrant,Database],['NVIDIA',data?.nvidia,Cpu],['GitHub',data?.github,Github]]; return <div className="space-y-6"><div><p className="text-sm text-indigo-300">Admin</p><h1 className="text-3xl font-black text-white">System Health</h1><p className="mt-2 text-sm text-zinc-500">Operational control plane for infrastructure, integrations, and logs.</p></div><div className="grid gap-4 md:grid-cols-5">{services.map(([name,ok,Icon]:any)=><div key={name} className="rounded-2xl border border-zinc-800 bg-zinc-900/60 p-5"><Icon className={ok?'text-emerald-400':'text-rose-400'} size={22}/><p className="mt-3 font-bold text-white">{name}</p><p className="text-xs text-zinc-500">{ok?'Healthy':'Attention required'}</p></div>)}</div><section className="rounded-2xl border border-zinc-800 bg-zinc-900/60 p-6"><h2 className="font-bold text-white"><Activity className="mr-2 inline" size={17}/>Resource Usage</h2><div className="mt-4 grid gap-4 md:grid-cols-2"><Bar label="CPU" value={32}/><Bar label="Memory" value={58}/></div></section><section className="rounded-2xl border border-zinc-800 bg-zinc-950 p-5 font-mono text-xs text-zinc-400">Live logs should be connected to Spring Boot actuator/log aggregation in production. Current API health is polling /api/status every 30s.</section></div>}
function Bar({label,value}:any){return <div><div className="mb-2 flex justify-between text-sm"><span>{label}</span><span>{value}%</span></div><div className="h-2 rounded-full bg-zinc-800"><div className="h-2 rounded-full bg-indigo-500" style={{width:`${value}%`}}/></div></div>}
